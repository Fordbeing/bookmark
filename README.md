# 📚 BookUtil - 全平台书签管理系统

<p align="center">
  <img src="https://img.shields.io/badge/Java-17-orange?logo=openjdk" alt="Java 17">
  <img src="https://img.shields.io/badge/Spring%20Boot-3.1.5-brightgreen?logo=springboot" alt="Spring Boot">
  <img src="https://img.shields.io/badge/Vue.js-3.5-4FC08D?logo=vuedotjs" alt="Vue 3">
  <img src="https://img.shields.io/badge/MySQL-8.0-4479A1?logo=mysql" alt="MySQL">
  <img src="https://img.shields.io/badge/Redis-latest-DC382D?logo=redis" alt="Redis">
  <img src="https://img.shields.io/badge/Elasticsearch-8.x-005571?logo=elasticsearch" alt="Elasticsearch">
  <img src="https://img.shields.io/badge/License-MIT-blue" alt="License">
</p>

<p align="center">
  <b>🌐 Web端 | 📱 微信小程序 | 🧩 Chrome扩展 | 🛠️ 管理后台</b>
</p>

---

一个功能完善的**全平台书签管理系统**，支持 Web、微信小程序、Chrome 浏览器扩展，帮助用户高效管理和同步书签。

## 📋 目录

- [项目架构](#-项目架构)
- [功能特性](#-功能特性)
- [技术栈详解](#-技术栈详解)
- [数据库设计](#-数据库设计)
- [核心模块分析](#-核心模块分析)
- [快速开始](#-快速开始)
- [API 接口](#-api-接口)
- [部署指南](#-部署指南)

---

## 🏗️ 项目架构

```
bookmark/
├── bookmark-backend/          # 后端服务 (Spring Boot 3.1.5)
│   ├── src/main/java/com/bookmark/
│   │   ├── controller/        # 19个控制器
│   │   ├── service/           # 27个服务类
│   │   ├── entity/            # 14个实体类
│   │   ├── mapper/            # MyBatis-Plus Mapper
│   │   ├── dto/               # 数据传输对象
│   │   ├── security/          # JWT认证
│   │   └── config/            # 配置类
│   ├── docker-compose.yml     # Docker编排
│   └── schema.sql             # 数据库初始化脚本
│
├── bookmark-frontend/         # Web前端 (Vue 3 + Element Plus)
│   └── src/
│       ├── components/        # 23个Vue组件
│       ├── api/               # 14个API模块
│       └── App.vue            # 主应用 (1200+行)
│
├── bookmark-admin/            # 管理后台 (Vue 3 + ECharts)
│   └── src/
│       ├── views/             # 7个视图模块
│       ├── api/               # API层
│       └── stores/            # Pinia状态管理
│
├── bookmark-miniprogram/      # 微信小程序
│   └── pages/                 # 6个页面
│       ├── index/             # 首页（书签列表）
│       ├── login/             # 登录
│       ├── add/               # 添加/编辑书签
│       ├── category/          # 分类管理
│       ├── trash/             # 回收站
│       └── profile/           # 个人中心
│
└── bookmark-extension/        # Chrome扩展 (Manifest V3)
    ├── popup.html/js/css      # 弹出窗口
    ├── background.js          # 后台服务
    └── api.js                 # API封装
```

### 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        客户端层                                   │
├─────────────┬─────────────┬─────────────┬─────────────────────────┤
│  Web前端    │  管理后台    │  微信小程序  │    Chrome扩展           │
│  (Vue3)     │  (Vue3)     │  (原生)      │    (Manifest V3)       │
└──────┬──────┴──────┬──────┴──────┬──────┴───────────┬─────────────┘
       │             │             │                  │
       └─────────────┴──────┬──────┴──────────────────┘
                            │ RESTful API (HTTPS)
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                     API Gateway / Nginx                          │
└──────────────────────────────┬──────────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────────┐
│                    Spring Boot 后端服务                           │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Controller层 (19个)                                        │ │
│  │  ├─ AuthController (登录/注册/Token刷新)                     │ │
│  │  ├─ BookmarkController (书签CRUD/回收站/置顶)                │ │
│  │  ├─ CategoryController / TagController                     │ │
│  │  ├─ SearchController (Elasticsearch全文搜索)                │ │
│  │  ├─ ShareController (分享/批量分享)                          │ │
│  │  ├─ Admin*Controller (管理后台API)                          │ │
│  │  └─ UserAnalyticsController (数据统计)                      │ │
│  └────────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Service层 (27个)                                           │ │
│  │  ├─ TokenService (JWT令牌管理)                              │ │
│  │  ├─ WxAuthService (微信登录)                                │ │
│  │  ├─ SearchService (ES搜索)                                  │ │
│  │  ├─ LinkHealthCheckService (死链检测)                       │ │
│  │  ├─ TrashCleanupTask (回收站自动清理)                        │ │
│  │  └─ ActivationCodeService (激活码管理)                      │ │
│  └────────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Security层                                                 │ │
│  │  ├─ JwtAuthenticationFilter (JWT拦截器)                     │ │
│  │  └─ SecurityConfig (Spring Security配置)                    │ │
│  └────────────────────────────────────────────────────────────┘ │
└──────────────┬───────────────┬───────────────┬──────────────────┘
               │               │               │
       ┌───────▼───────┐ ┌─────▼─────┐ ┌───────▼───────┐
       │    MySQL 8.0   │ │   Redis   │ │ Elasticsearch │
       │   (持久化存储)  │ │ (缓存/Token)│ │  (全文搜索)   │
       └───────────────┘ └───────────┘ └───────────────┘
```

---

## ✨ 功能特性

### 1. 用户认证系统

| 功能 | 描述 |
|------|------|
| **邮箱登录** | 支持邮箱+密码登录，BCrypt加密存储 |
| **微信登录** | 小程序端微信授权一键登录 |
| **手机号绑定** | 多平台账号绑定 |
| **JWT双Token** | Access Token (24h) + Refresh Token (7d) |
| **Token刷新** | 无感刷新，Redis存储Token状态 |
| **登录历史** | 记录登录IP、设备、时间 |
| **登录锁定** | 错误次数限制，防暴力破解 |

### 2. 书签管理

| 功能 | 描述 |
|------|------|
| **基础CRUD** | 创建、读取、更新、删除书签 |
| **分类管理** | 自定义分类，支持颜色和图标 |
| **标签系统** | 多标签支持，JSON格式存储 |
| **收藏功能** | 星标重要书签 |
| **置顶功能** | 置顶常用书签 |
| **回收站** | 软删除，30天自动清理 |
| **批量操作** | 批量删除、批量分享 |
| **访问统计** | 记录每个书签的访问次数 |

### 3. 搜索功能

| 功能 | 描述 |
|------|------|
| **全文搜索** | Elasticsearch驱动，搜索标题/URL/描述 |
| **高级筛选** | 按分类、标签、日期、域名筛选 |
| **死链检测** | 定时检测书签有效性 (linkStatus: 0-未检测 1-正常 2-失效 3-重定向 4-超时) |

### 4. 分享功能

| 功能 | 描述 |
|------|------|
| **单链接分享** | 生成唯一分享码 |
| **批量分享** | 多书签打包分享 |
| **密码保护** | 可选密码访问 |
| **过期时间** | 设置分享链接有效期 |
| **访问统计** | 统计分享链接访问次数 |

### 5. 管理后台

| 功能 | 描述 |
|------|------|
| **仪表盘** | 用户/书签增长趋势图表 (ECharts) |
| **用户管理** | 用户列表、状态管理、详情查看 |
| **激活码系统** | 生成/管理激活码，扩展用户限额 |
| **系统设置** | 登录开关、维护模式、公告管理 |
| **操作日志** | 管理员操作审计 |
| **系统监控** | MySQL/Redis/ES健康状态 |

### 6. 个性化设置

| 功能 | 描述 |
|------|------|
| **主题切换** | Light / Dark / Auto |
| **颜色定制** | 主色、强调色、侧边栏渐变色 |
| **显示模式** | 卡片 / 列表 / 紧凑视图 |
| **新标签页** | 可配置是否新标签页打开 |

---

## 🛠️ 技术栈详解

### 后端 (bookmark-backend)

| 技术 | 版本 | 用途 |
|------|------|------|
| **Spring Boot** | 3.1.5 | 核心框架 |
| **Spring Security** | 6.x | 安全认证框架 |
| **MyBatis-Plus** | 3.5.7 | ORM框架 |
| **MySQL** | 8.0 | 主数据库 |
| **Redis** | latest | Token缓存、分布式锁 |
| **Elasticsearch** | 8.x | 全文搜索 |
| **JWT (jjwt)** | 0.12.3 | 令牌认证 |
| **Hutool** | 5.8.25 | 工具库 |
| **Knife4j** | 4.4.0 | API文档 |
| **Lombok** | 1.18.30 | 代码简化 |

### 前端 (bookmark-frontend)

| 技术 | 版本 | 用途 |
|------|------|------|
| **Vue.js** | 3.5.24 | 前端框架 |
| **Element Plus** | 2.12.0 | UI组件库 |
| **Vue Router** | 4.6.4 | 路由管理 |
| **Axios** | 1.13.2 | HTTP客户端 |
| **TailwindCSS** | 3.4.17 | 原子化CSS |
| **Vite** | 7.2.4 | 构建工具 |

### 管理后台 (bookmark-admin)

| 技术 | 版本 | 用途 |
|------|------|------|
| **Vue.js** | 3.3.11 | 前端框架 |
| **ECharts** | 5.4.3 | 数据可视化 |
| **Pinia** | 2.1.7 | 状态管理 |
| **Day.js** | 1.11.10 | 日期处理 |
| **Vite** | 5.0.8 | 构建工具 |

### 微信小程序 (bookmark-miniprogram)

| 技术 | 用途 |
|------|------|
| **微信原生开发** | 小程序框架 |
| **wx.request** | API请求 |
| **TabBar导航** | 底部导航 |
| **下拉刷新** | 数据刷新 |
| **滑动删除** | 移动端交互 |

### Chrome扩展 (bookmark-extension)

| 技术 | 用途 |
|------|------|
| **Manifest V3** | 扩展规范 |
| **Service Worker** | 后台服务 |
| **chrome.storage** | 本地存储 |
| **快捷键** | Ctrl+Shift+S 快速保存 |

---

## 🗄️ 数据库设计

### ER关系图

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│    user     │       │  bookmark   │       │  category   │
├─────────────┤       ├─────────────┤       ├─────────────┤
│ id (PK)     │◄──────│ user_id (FK)│       │ id (PK)     │
│ username    │       │ id (PK)     │───────│ user_id (FK)│
│ email       │       │ title       │       │ name        │
│ password    │       │ url         │       │ color       │
│ phone       │       │ description │       │ icon        │
│ openid      │       │ icon_url    │       │ sort_order  │
│ unionid     │       │ category_id │───────│ create_time │
│ login_type  │       │ tags (JSON) │       │ update_time │
│ avatar      │       │ is_favorite │       └─────────────┘
│ nickname    │       │ is_pinned   │
│ status      │       │ visit_count │       ┌─────────────┐
│ is_admin    │       │ link_status │       │    tag      │
│ create_time │       │ status      │       ├─────────────┤
│ update_time │       │ create_time │       │ id (PK)     │
│ last_login  │       │ update_time │       │ user_id (FK)│
└─────────────┘       └─────────────┘       │ name        │
      │                                      │ color       │
      │                                      │ usage_count │
      ▼                                      └─────────────┘
┌─────────────┐       ┌──────────────────┐
│user_settings│       │shared_bookmarks  │
├─────────────┤       ├──────────────────┤
│ id (PK)     │       │ id (PK)          │
│ user_id (FK)│       │ user_id (FK)     │
│ theme       │       │ title            │
│ primary_clr │       │ bookmark_ids     │
│ display_mode│       │ share_code       │
│ auto_new_tab│       │ password         │
│ show_stats  │       │ expire_time      │
│ bookmark_lmt│       │ view_count       │
│ category_lmt│       │ status           │
└─────────────┘       └──────────────────┘

┌─────────────────┐   ┌─────────────────┐
│ activation_code │   │ user_activation │
├─────────────────┤   ├─────────────────┤
│ id (PK)         │   │ id (PK)         │
│ code            │◄──│ code_id (FK)    │
│ extra_bookmarks │   │ user_id (FK)    │
│ extra_categories│   │ extra_bookmarks │
│ expire_days     │   │ extra_categories│
│ max_uses        │   │ expire_time     │
│ used_count      │   │ status          │
│ created_by      │   │ create_time     │
│ status          │   └─────────────────┘
│ create_time     │
└─────────────────┘
```

### 核心表字段说明

#### user 表
```sql
CREATE TABLE user (
  id             BIGINT AUTO_INCREMENT PRIMARY KEY,
  username       VARCHAR(50) NOT NULL UNIQUE,
  email          VARCHAR(100) UNIQUE,          -- 邮箱登录可空(微信登录)
  password       VARCHAR(255),                 -- BCrypt加密
  phone          VARCHAR(20) UNIQUE,           -- 手机号绑定
  openid         VARCHAR(100) UNIQUE,          -- 微信OpenID
  unionid        VARCHAR(100),                 -- 微信UnionID
  login_type     TINYINT DEFAULT 1,            -- 1-邮箱 2-微信 3-手机
  avatar         VARCHAR(500),
  nickname       VARCHAR(50),
  status         TINYINT DEFAULT 1,            -- 0-禁用 1-正常
  is_admin       TINYINT DEFAULT 0,            -- 0-普通 1-管理员
  create_time    DATETIME DEFAULT CURRENT_TIMESTAMP,
  update_time    DATETIME ON UPDATE CURRENT_TIMESTAMP,
  last_login_time DATETIME
);
```

#### bookmark 表
```sql
CREATE TABLE bookmark (
  id             BIGINT AUTO_INCREMENT PRIMARY KEY,
  user_id        BIGINT NOT NULL,
  title          VARCHAR(255) NOT NULL,
  url            VARCHAR(2048) NOT NULL,
  description    TEXT,
  icon_url       VARCHAR(500),
  category_id    BIGINT,
  tags           VARCHAR(500),                 -- JSON数组 ["tag1","tag2"]
  is_favorite    TINYINT DEFAULT 0,            -- 0-否 1-是
  is_pinned      TINYINT DEFAULT 0,            -- 0-否 1-是
  visit_count    INT DEFAULT 0,
  sort_order     INT DEFAULT 0,
  link_status    TINYINT DEFAULT 0,            -- 0-未检测 1-正常 2-失效 3-重定向 4-超时
  last_check_time DATETIME,
  check_message  VARCHAR(500),
  status         TINYINT DEFAULT 1,            -- 0-删除(回收站) 1-正常
  create_time    DATETIME DEFAULT CURRENT_TIMESTAMP,
  update_time    DATETIME ON UPDATE CURRENT_TIMESTAMP
);
```

---

## 🔧 核心模块分析

### 1. 认证模块 (AuthController)

```
POST /auth/register     - 用户注册
POST /auth/login        - 邮箱登录
POST /auth/admin-login  - 管理员登录
POST /auth/wx-login     - 微信小程序登录
POST /auth/bind-phone   - 绑定手机号
POST /auth/refresh      - 刷新Access Token
POST /auth/logout       - 注销登录
GET  /auth/me           - 获取当前用户信息
```

**JWT Token机制：**
- Access Token: 24小时有效，用于API认证
- Refresh Token: 7天有效，用于刷新Access Token
- Redis存储Token ID，支持主动失效

### 2. 书签模块 (BookmarkController)

```
GET    /bookmarks                  - 分页获取书签列表
GET    /bookmarks/{id}             - 获取书签详情
POST   /bookmarks                  - 创建书签
PUT    /bookmarks/{id}             - 更新书签
DELETE /bookmarks/{id}             - 删除书签(移入回收站)
DELETE /bookmarks/batch            - 批量删除
PUT    /bookmarks/{id}/favorite    - 更新收藏状态
PUT    /bookmarks/{id}/visit       - 增加访问次数
PUT    /bookmarks/{id}/pin         - 置顶书签
PUT    /bookmarks/{id}/unpin       - 取消置顶
GET    /bookmarks/pinned           - 获取置顶书签
GET    /bookmarks/trash            - 获取回收站
PUT    /bookmarks/{id}/restore     - 恢复书签
DELETE /bookmarks/{id}/permanent   - 永久删除
DELETE /bookmarks/trash/clear      - 清空回收站
GET    /bookmarks/advanced-search  - 高级搜索
GET    /bookmarks/dead-links       - 获取失效链接
```

### 3. 管理后台模块

```
# 仪表盘
GET /admin/dashboard/overview       - 总览数据
GET /admin/dashboard/trends         - 趋势图表数据
GET /admin/dashboard/recent         - 最近活动

# 用户管理
GET    /admin/users                 - 用户列表
GET    /admin/users/{id}            - 用户详情
PUT    /admin/users/{id}/status     - 更新用户状态
DELETE /admin/users/{id}            - 删除用户

# 激活码管理
GET    /admin/activation-codes      - 激活码列表
POST   /admin/activation-codes      - 创建激活码
DELETE /admin/activation-codes/{id} - 删除激活码

# 系统设置
GET    /admin/system/config         - 获取系统配置
PUT    /admin/system/config         - 更新系统配置
PUT    /admin/system/maintenance    - 维护模式开关
```

### 4. 前端组件结构 (bookmark-frontend)

| 组件 | 功能 |
|------|------|
| `App.vue` | 主应用（1200+行），包含书签列表、过滤、排序、批量操作 |
| `AuthPage.vue` | 登录/注册页面 |
| `SidebarNew.vue` | 侧边栏导航，分类/收藏/回收站筛选 |
| `Navbar.vue` | 顶部导航栏，搜索功能 |
| `AddBookmarkModal.vue` | 快速添加书签弹窗 |
| `AddBookmarkModal2.vue` | 手工添加书签弹窗 |
| `EditBookmarkModal.vue` | 编辑书签弹窗 |
| `SettingsPage.vue` | 设置页面（主题、颜色、显示模式） |
| `ProfilePage.vue` | 个人中心 |
| `AdvancedSearchDrawer.vue` | 高级搜索抽屉 |
| `AnalyticsDrawer.vue` | 数据统计抽屉 |
| `DeadLinksDrawer.vue` | 失效链接管理 |
| `ShareModal.vue` | 分享弹窗 |
| `BatchShareModal.vue` | 批量分享弹窗 |
| `DataManagementPage.vue` | 数据导入导出 |
| `ExtensionPage.vue` | 浏览器扩展下载页 |

---

## 🚀 快速开始

### 环境要求

| 软件 | 版本 | 必需 |
|------|------|------|
| JDK | 17+ | ✅ |
| Node.js | 18+ | ✅ |
| MySQL | 8.0+ | ✅ |
| Redis | 6+ | ✅ |
| Elasticsearch | 8.x | ⚠️ (搜索功能需要) |
| Docker | 20+ | 📌 (推荐) |

### 方式一：Docker 一键部署 (推荐)

```bash
# 1. 克隆项目
git clone https://github.com/yourusername/bookmark.git
cd bookmark/bookmark-backend

# 2. 启动所有服务 (MySQL + Redis + Elasticsearch + App)
docker-compose up -d

# 3. 查看日志
docker-compose logs -f app

# 4. 访问应用
# 后端API: http://localhost:8080
# API文档: http://localhost:8080/doc.html
```

**docker-compose.yml 服务说明：**
```yaml
services:
  mysql:      # 端口 3306, 数据库 bookmark_db
  redis:      # 端口 6379
  elasticsearch:  # 端口 9200/9300
  app:        # 端口 8080, Spring Boot应用
```

### 方式二：手动部署

#### 1. 启动后端服务

```bash
cd bookmark-backend

# 1) 初始化数据库
mysql -u root -p < schema.sql

# 2) 配置 application.yml
# 修改 MySQL/Redis/ES 连接信息

# 3) 构建并运行
mvn clean package -DskipTests
java -jar target/bookmark-backend-1.0.0.jar
```

**后端配置文件 (application.yml)：**
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bookmark_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: your_password
    
  data:
    redis:
      host: localhost
      port: 6379
    elasticsearch:
      uris: http://localhost:9200

jwt:
  secret: your-256-bit-secret-key-here
  access-expiration: 86400000    # 24小时
  refresh-expiration: 604800000  # 7天
```

#### 2. 启动 Web 前端

```bash
cd bookmark-frontend

# 1) 安装依赖
npm install

# 2) 配置API地址 (.env.development)
VITE_API_BASE_URL=http://localhost:8080

# 3) 启动开发服务器
npm run dev

# 4) 生产构建
npm run build
```

#### 3. 启动管理后台

```bash
cd bookmark-admin
npm install
npm run dev
```

#### 4. 安装 Chrome 扩展

1. 打开 Chrome，进入 `chrome://extensions/`
2. 开启右上角「开发者模式」
3. 点击「加载已解压的扩展程序」
4. 选择 `bookmark-extension` 目录
5. 配置扩展程序中的 API 地址

**快捷键：**
- `Ctrl+Shift+S` (Windows/Linux)
- `Cmd+Shift+S` (Mac)
- 快速保存当前页面为书签

#### 5. 微信小程序

1. 下载 [微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)
2. 导入 `bookmark-miniprogram` 目录
3. 在 `api/request.js` 中配置后端 API 地址
4. 在 `project.config.json` 中配置 AppID

---

## � API 接口

### 接口文档

后端集成 Knife4j (Swagger增强)，启动后访问：

```
http://localhost:8080/doc.html
```

### 接口模块一览

| 模块 | 路径前缀 | 说明 |
|------|----------|------|
| 认证 | `/auth/*` | 登录/注册/Token刷新 |
| 书签 | `/bookmarks/*` | 书签CRUD/回收站/置顶 |
| 分类 | `/categories/*` | 分类管理 |
| 标签 | `/tags/*` | 标签管理 |
| 搜索 | `/search/*` | Elasticsearch全文搜索 |
| 分享 | `/share/*` | 书签分享 |
| 公开分享 | `/public/*` | 无需认证的分享访问 |
| 设置 | `/settings/*` | 用户设置 |
| 统计 | `/analytics/*` | 使用数据统计 |
| 激活码 | `/activation-codes/*` | 用户激活码 |
| 管理后台 | `/admin/*` | 管理员API |

### 通用响应格式

```json
{
  "code": 200,
  "message": "操作成功",
  "data": { ... }
}
```

### 认证方式

所有需要认证的接口，请在请求头添加：

```
Authorization: Bearer <access_token>
```

---

## � 部署指南

### Nginx 配置示例

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    # 前端静态文件
    location / {
        root /var/www/bookmark-frontend/dist;
        try_files $uri $uri/ /index.html;
    }
    
    # 管理后台
    location /admin {
        alias /var/www/bookmark-admin/dist;
        try_files $uri $uri/ /admin/index.html;
    }
    
    # 后端API代理
    location /api {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

### 生产环境检查清单

- [ ] MySQL 配置主从复制
- [ ] Redis 配置持久化 (RDB/AOF)
- [ ] Elasticsearch 配置分片和副本
- [ ] 配置 HTTPS 证书
- [ ] 配置防火墙规则
- [ ] 配置日志收集 (ELK)
- [ ] 配置监控告警 (Prometheus + Grafana)
- [ ] 设置数据库定时备份

---

## 🤝 贡献指南

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add AmazingFeature'`)
4. 推送分支 (`git push origin feature/AmazingFeature`)
5. 提交 Pull Request

---

## 📄 开源协议

本项目采用 [MIT License](LICENSE) 开源协议。

---

## 📞 联系方式

如有问题或建议，欢迎：
- 提交 [Issue](../../issues)
- 发起 [Discussion](../../discussions)

---

<p align="center">
  ⭐ 如果这个项目对你有帮助，请点个 Star 支持一下！
</p>
